using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Weapon : MonoBehaviour {
    //private bool dropped = false;
    //private float pending;
    //public static float StartTimer,lastTimer;
    //public static bool isStartTimer = false;

    //public void drop(bool condition,float pending)
    //{
    //    this.dropped = true;
    //    this.pending = pending;
    //}

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
  //      if(isStartTimer) lastTimer = Time.time;

  //      //Debug.Log(lastTimer + "- " + StartTimer + " =" + (lastTimer - StartTimer));
		//if(dropped == true && lastTimer-StartTimer >= pending)
  //      {
  //          gameObject.GetComponent<Rigidbody>().useGravity = true;
  //          gameObject.GetComponent<Collider>().enabled = true;
  //      }
	}
}
